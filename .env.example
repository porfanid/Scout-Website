# Google Drive API Configuration
# Required environment variables for Google Drive integration

# To enable Google Drive file picker in events:
# 1. Create a project in Google Cloud Console
# 2. Enable the Google Drive API
# 3. Create credentials (API key and OAuth 2.0 client ID)
# 4. Add the following environment variables to your .env file:

# VITE_GOOGLE_DRIVE_API_KEY=your_google_drive_api_key_here
# VITE_GOOGLE_DRIVE_CLIENT_ID=your_google_drive_client_id_here

# Note: If these variables are not set, the Google Drive integration will be disabled
# and users will only see the standard event creation form without file upload options.